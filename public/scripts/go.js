/*banner.addEventListener("click", async () => {
    console.log("yolo");

    const api_url = `/lol`;
    const response = await fetch(api_url);
    const json = await response.json();
    console.log(json);
});
*/
